{
  "name": "web-map-editor",
  "private": true,
  "version": "4.0.0-phase0",
  "description": "Indoor Navigation Web Map Editor — Phase 0 core tests",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
